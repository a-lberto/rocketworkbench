# Test structure with edge cases and comments
root = (
    # Simple values
    key1 = 123,
    # String with quotes
    key2 = "quoted string",
    # Nested parent with siblings
    parent1 = (
        sub1 = 4.56,
        sub2 = 7.89
    ),
    # Multiple levels and nodes with same key
    level1 = (
        nested = 1,
        nested = 2
    ),
    # Comments inside parent
    parent2 = (
        # Comment
        sub3 = 10.0
    )
)
